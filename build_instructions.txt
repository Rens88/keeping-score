Build Instructions Log
Last updated: 2026-04-18

Purpose
- Keep a running summary of requested product/code changes so new requests can be checked for alignment with prior decisions.

Requested Changes (summarized)
1. Leaderboard:
- Add a bonus-points column and a total-points column.
- Define bonus points as points above/below default win/draw/loss base points.

2. Past matches view:
- Make the overview compact (single-row style per match).
- Show who played whom, result, and points gained.
- Color-code personal outcome: win green, draw blue, loss red.

3. Profile/light-dark readability:
- Fix visibility issues where metric values/labels became unreadable in some light/dark states.

4. Release metadata workflow and display:
- Add an agent rule to ask for explicit "Update" before editing `release_info.json`.
- Show release metadata at the bottom of the app: nickname (left), version (center), date (right).

5. Global theme simplification:
- Use a simpler color scheme that works in both light and dark contexts.
- Primary palette direction: orange, white, black (with supporting neutral tones).
- Ensure form instructions, field labels, field values, tabs, and status messages remain readable.

6. Agent memory and conflict behavior:
- Add an instruction that all requested changes are logged.
- Before implementing new requests, check against prior logged requests.
- If there is a conflict, ask for clarification before implementing.

7. File format correction:
- Use `build_instructions.txt` for this log (not `build_instructions.py`).

8. Re-confirmation of file format:
- Reaffirm that request-history logging must stay in `build_instructions.txt` only.

9. Release metadata update command:
- User explicitly said "update", so `release_info.json` was updated.
- Patch version was incremented by 1 and release date kept at today's date.
- Nickname was set to summarize the latest most important changes.

10. Default data-source configuration:
- Set repository defaults to load database state from `demo_state`.
- Updated default `DB_PATH` to `demo_state/weekend_tracker_requested_demo_state.sqlite3`.
- Updated README examples/documentation so defaults match runtime behavior.

11. Release metadata update command:
- User explicitly said "update" again, so `release_info.json` was updated again.
- Patch version was incremented by 1 and date remained today's date.
- Nickname was updated to reflect the latest major change (default demo-state loading).

12. Dual-deployment refinement (local LAN + Streamlit Cloud):
- Refine the same multipage Streamlit codebase to support both local Wi-Fi hosting and Streamlit Community Cloud.
- Add environment-aware config (`APP_ENV`, DB defaults per environment, secrets/env-driven admin bootstrap).
- Remove hardcoded admin bootstrap defaults and enforce complete seed values only when needed.
- Add local LAN defaults in `.streamlit/config.toml`, helper run scripts, and smoke test script.
- Improve portability by removing machine-specific script paths.
- Rewrite README with explicit setup/run/deploy/config/storage guidance for both modes.

13. Release metadata update command:
- User explicitly said "update" again and requested release metadata update.
- Patch version was incremented by 1, date remained today's date, and nickname was refreshed to summarize the latest platform-readiness work.

14. Windows LAN startup support:
- Clarify how to run the LAN startup script on Windows.
- Add a dedicated `scripts/run_lan.bat` launcher for Windows CMD users.
- Update README LAN instructions to include Bash, PowerShell, and CMD startup options.

15. Verbose Windows launcher behavior:
- Make the Windows `.bat` launcher verbose with explicit step logging, similar to provided reference style.
- Ensure the `.bat` always pauses before closing, including on failure.

16. LAN verification troubleshooting:
- Provide exact steps to verify laptop and phone are on the same local subnet (expected `192.168.2.x`).
- Clarify which address to use from phone browser (`http://<laptop-lan-ip>:8501`, not `0.0.0.0`).
- Provide practical checks for Wi-Fi details, VPN/mobile-data interference, and router client isolation.

17. Firewall validation and next diagnostics:
- Confirm Windows inbound firewall rule for TCP 8501 is correctly created on the Private profile.
- Continue with targeted diagnostics to isolate whether remaining failure is due to router client isolation, wrong active network profile, or app-specific blocking.

18. Non-Streamlit LAN connectivity test result:
- User confirmed a plain Python HTTP server on port 9999 was also unreachable from phone.
- Use this as evidence to prioritize LAN path diagnostics (firewall scope for test port, device isolation, or route/profile mismatch) over Streamlit-specific fixes.

19. External second-opinion request:
- Prepare a concise, high-context troubleshooting prompt the user can paste into another LLM.
- Include attempted steps, confirmed facts, and request new diagnostic ideas without repeating already-tried basics.

20. LAN connectivity debugging outcome (other-LLM summary):
- Goal:
  - Access a locally hosted Streamlit app (`0.0.0.0:8501`) from another device (phone) on the same Wi-Fi network.
- Observed behavior:
  - App runs correctly on laptop (`localhost` works).
  - App not accessible from phone via `http://<local-ip>:port`.
  - Same issue reproduced with simple server (`python -m http.server`), confirming issue is not Streamlit-specific.
- Key diagnostic findings:
  - Same subnet (`192.168.2.x`), devices visible in router.
  - Correct bind (`0.0.0.0`) and listening ports confirmed.
  - Critical test: firewall ON -> connection fails; firewall OFF -> connection works.
  - Conclusion: Windows Firewall is the blocker.
- Root cause:
  - Windows Defender Firewall had auto-generated inbound `Block` rules for `python3.12.exe` on `Private, Public` profiles.
  - These can be created when Python first opens a listening socket and access is denied.
- Important firewall behavior:
  - Block rules override allow rules.
  - Allow rules (including specific ports/subnets) do not work while conflicting block rules exist.
  - "Query User" style rules can persist with high precedence.
- Fix applied:
  - Removed blocking Python inbound rules:
    - `Get-NetFirewallRule -Enabled True -Direction Inbound -Action Block | Where-Object {$_.DisplayName -eq "Python"} | Remove-NetFirewallRule`
  - Added clean program-based allow rule:
    - `New-NetFirewallRule -DisplayName "Allow Python LAN (Private)" -Direction Inbound -Action Allow -Program "<python.exe path>" -Protocol TCP -Profile Private -RemoteAddress 192.168.2.0/24`
- Final result:
  - Firewall ON and LAN access works from phone.
  - Works for both `http.server` and Streamlit.
- Key lessons:
  - Always test with `python -m http.server` to separate app vs network issues.
  - If bind/subnet are correct and firewall OFF works, suspect firewall rule precedence.
  - Check block rules first (`Get-NetFirewallRule -Action Block`), then allow rules.
  - Prefer program-level allow rules, scoped to Private profile and local subnet.
  - Windows Store Python (`WindowsApps`) can produce confusing firewall rule behavior.
- Classification:
  - Category: Networking -> Local LAN -> Firewall.
  - Failure mode: Silent block due to hidden high-precedence rule.
  - Detection signal: Works when firewall OFF, fails otherwise.
- Recommended future setup:
  - Keep one clean allow rule for Python executable.
  - Scope to Private network and local subnet.
  - Avoid duplicate/conflicting rules.
  - Periodically clean firewall rules.

21. Comprehensive UI readability refresh:
- Prioritize readable contrast across light/dark contexts over strict color preference, while still leaning on an orange, white, and black-friendly palette.
- Strengthen shared theme styling for titles, body text, captions, helper instructions, inputs, selects, uploaders, toggles, tabs, alerts, metrics, tables, and button states.
- Add consistent page intro cards so page titles and instructions stay visible and easy to scan.
- Make the top quick-navigation buttons more mobile-friendly by splitting them into shorter rows instead of one cramped strip.
- Improve clarity on login, invitation, and profile screens with clearer section framing and guidance copy.

22. Header/mobile layout and compact leaderboard follow-up:
- Fix the broken page-intro/header rendering where raw HTML appeared in the UI.
- Improve the top header so the logo/banner layout looks intentional on mobile instead of stacking awkwardly.
- Stop forcing light mode and make the shared theme adapt better to both light and dark environments.
- Replace the wide leaderboard dataframe with a narrower responsive layout that keeps name + motto together and fits mobile screens without horizontal scrolling.
- Strengthen README LAN troubleshooting with the previously requested subnet, host-IP, VPN/mobile-data, and firewall-rule guidance.
- Produce a robust implementation plan for keeping users logged in across page refreshes, without shipping an unsafe quick fix first.

23. Page-intro rendering and dark-mode label readability correction:
- Fix the shared page-intro rendering path so hard-coded HTML blocks are rendered as HTML instead of appearing as literal markup/code text.
- Route shared banner/intro/footer HTML through a direct HTML renderer when Streamlit supports it, with a safe markdown fallback.
- Strengthen label, caption, and helper-text contrast in the shared theme so login/form text stays readable in dark mode instead of fading into the background.

24. Release metadata update command:
- User explicitly said "update", so `release_info.json` was updated again.
- Patch version was incremented by 1 and date remained today's date.
- Nickname was refreshed to summarize the latest readability and header fixes.

25. Mobile form-label readability hardening:
- Stop relying on Streamlit's built-in widget labels for the main forms where labels remained too dim on dark mobile screens.
- Add shared high-contrast custom field-label styling and render visible labels above collapsed Streamlit widgets.
- Apply the custom labels across login, invitation, profile, doubler selection, and admin form screens so text input/select labels stay readable in dark mode.

26. Mobile UI regression cleanup from screenshot review:
- Replace profile `st.metric` usage with custom stat tiles so values and labels stay readable on dark mobile screens.
- Darken and rebalance dropdown/listbox, uploader-button, checkbox, and toggle styling so controls are readable without bright white panels or near-invisible labels.
- Stop using a dropdown for the participant doubler picker when it hurts mobile readability; use a clearer single-option or radio-style flow instead.
- Route leaderboard and compact past-match HTML through the shared direct HTML renderer so rows cannot fall back into literal markup/table-like rendering again.

27. Home page shortcut cleanup:
- Remove the bottom home-page participant shortcut buttons for opening leaderboard, upcoming matches, and profile.
- Keep navigation available through the existing quick navigation and sidebar instead of duplicating those buttons on the home screen.

28. Leaderboard compact-expand behavior:
- Show only the rank, photo, name, motto, and total points in the default leaderboard row.
- Hide secondary stats by default and reveal them only when the user clicks the player name area to expand that row.
- Keep the leaderboard mobile-friendly while reducing default visual density.

29. Registration overhaul and registration game:
- Replace the participant self-service invite-token flow with an admin-managed registration flow where admins create participant accounts, choose passwords, and generate a funny shareable invitation message containing the web link, username, and password.
- Route newly created participants to an "And now we wait..." page until the admin activates the registration game.
- Add a dedicated admin registration-game page with an activation toggle and placeholder question/hint preview.
- Add a participant registration game that asks placeholder multiple-choice questions, reveals one hint per answered question, allows a location guess after each question, accepts "Erp" as the correct answer, awards `10 - incorrect answers` starting points, and links to the accommodation on success.
- Carry earned registration-game points into the leaderboard as the participant's starting ranking.

30. Registration game exact content and UX refinement:
- Replace the placeholder registration-game questions and hints with the exact 10 supplied Dutch multiple-choice questions, A/B/C/D option keys, correct-answer keys, and hints.
- Keep the game flow as one multiple-choice answer followed immediately by feedback, the hint, and one destination-guess input per question.
- Use four clear answer buttons instead of a single radio selector.
- Keep score as `10 - aantal fout beantwoorde vragen`, stop the game immediately when the user correctly guesses `Erp`, and make open guess validation trim spaces and ignore letter case.
- Ensure the final hint literally gives away the answer `Erp`.

31. Streamlit Cloud import hardening:
- Fix the online import failure around `app.py -> tournament_tracker.session` by removing an unnecessary runtime import of `AppServices` from `tournament_tracker.bootstrap`.
- Keep the bootstrap import available only for type checking, reducing the risk of fragile import chains during Streamlit Cloud startup.

32. Cloud leaderboard schema-compatibility fix:
- Harden repository startup so registration-game columns and `app_settings` are re-created idempotently even when a deployed database is missing them or has drifted away from the expected migration state.
- Make participant/profile queries fall back to safe legacy defaults for registration-game fields so leaderboard and participant pages can still load against older Cloud databases.

33. Windows LAN launcher port-availability refinement:
- Improve the verbose Windows CMD LAN launcher so it checks whether the chosen Streamlit port is already in use before starting.
- If the default port `8501` is occupied, automatically fall back to the next free nearby port and print the adjusted LAN URL guidance.
- If the user explicitly set `STREAMLIT_SERVER_PORT` and that port is busy, fail early with a clear instruction instead of letting Streamlit abort later.
- Update README LAN notes to mention the new automatic Windows `.bat` fallback behavior.

34. Streamlit deprecation cleanup for invitation preview:
- Remove the deprecated `st.components.v1.html` usage from the registration invitation preview flow.
- Keep the invitation message visible in a native Streamlit code block and instruct the admin to use the built-in copy icon or manual text selection instead of the old custom HTML/JS copy button.

35. Whack-a-mole planning and post-registration reward/info follow-up:
- Work out a concrete integration plan for a weekend-long `Whack-a-mole` minigame, including where players find it, how scores and the minigame leaderboard are tracked, how the admin sets the deadline, and which admin controls are needed for awarding leaderboard points later.
- Add a post-registration-game reward moment where `assets/reward_registration_game.png` is shown to participants who correctly guessed the location.
- Add a gated info page for successful registrants with the accommodation link, the supplied WieBetaaltWat link, and practical stay details (towels, bed linen, Friday 14:00 check-in, Saturday 15:00 check-out).

36. Whack-a-mole implementation and weekend-info rollout:
- Implement a simple embedded `Whack-a-mole` minigame with persistent run storage, a best-score minigame leaderboard, and automatic inclusion of admin-awarded minigame points in the main leaderboard total.
- Add a dedicated admin minigame page to configure active state, opening time, deadline, and comma-separated award scheme, plus a post-deadline action to apply or refresh weekend points from the current minigame standings.
- Add participant navigation and home links for the minigame and a new gated `Weekend Info` page.
- Show `assets/reward_registration_game.png` on the registration-game success screen, add a direct button to the new info page, and use the same shared weekend-info content for accommodation, WieBetaaltWat, and practical stay details.

37. Cached AppServices compatibility fix:
- Harden the new minigame/info pages against older Streamlit `cache_resource` entries by adding a runtime service loader that refreshes or rebuilds legacy `AppServices` objects missing the new `minigame_service`.
- Route the new weekend info and minigame pages through that runtime-safe bootstrap helper so the app can recover without manual cache clearing.

38. Admin minigame form readability fix:
- Apply the existing high-contrast custom field-label pattern to the Whack-a-mole admin configuration form instead of relying on Streamlit’s default widget labels.
- Collapse the built-in labels for the schedule and award-scheme inputs so dark-mode/mobile readability matches the previously hardened admin forms elsewhere in the app.

39. Unified competition-points architecture:
- Refactor leaderboard point aggregation so registration-game points and ranking-based minigame points flow through the same generic competition-points storage layer instead of being added in two unrelated ways.
- Add a reusable competition-awards model that can also support future multi-competitor/ranking games beyond the current Whack-a-mole setup.
- Keep head-to-head match points separate for now, but make the leaderboard combine those match points with the shared competition-awards total in a consistent way.

40. Match-layout, betting, and expanded specials overhaul:
- Refresh the past-matches layout so it no longer says won/lost/drew, shows earned points with one decimal plus the text `points earned`, and displays special icons next to the relevant participant names.
- Make the upcoming-matches view more compact and match-summary oriented, then add pre-start betting so participants can risk 1 or 2 already-earned points on `side 1 win`, `draw`, or `side 2 win`, with correct bets paying back double and zero-point users blocked from gambling.
- Expand specials into a fuller system with a dedicated in-app explanation page, current-availability highlighting per participant, and admin control over the catch-up threshold.
- Keep everyone starting with a doubler, reactivate it for whoever is last in the ranking, unlock double-or-nothing after a first win, unlock Wheel of Fortune after a second loss, and make catch-up mode automatically double betting and match-performance scoring when the ranking gap exceeds the admin-set threshold.
- Treat match-related modifiers and betting as the same kind of add-on scoring layer around base head-to-head points so the leaderboard, past-match display, and future multi-competitor/ranking competitions can build on a more consistent scoring model.

41. Release metadata update command:
- User explicitly said "Update", so `release_info.json` was updated.
- Patch version was incremented from `v0.0.8` to `v0.0.9`, the date was set to `2026-04-20`, and the nickname was refreshed to summarize the betting/specials release.

42. Admin debug reset tools and special override controls:
- Add admin debug tooling to load the bundled fake half-way snapshot directly from `demo_state/weekend_tracker_requested_demo_state.sqlite3`.
- Add a nuclear-style fresh reset that wipes participant usage data, invitations, matches, scores, minigame state, and participant accounts, while preserving the current admin account so the app remains accessible after the reset.
- Extend the admin specials page with a table showing which participant currently has which special available/active/used, plus persistent per-person overrides that can force a special on, force it off, or return it to the normal automatic rules.

43. Admin dashboard readability follow-up:
- Replace the remaining `st.metric` summary tiles on the main admin dashboard with the shared high-contrast custom stat tiles used elsewhere in the app, so participant/match counts stay readable on dark mobile screens.
- Route the admin dashboard through the runtime-safe services loader as well, matching the newer admin pages and avoiding stale cached service objects.

44. Invitation base-url fallback:
- Use `https://keeping-score-teamweekend-2025-2026.streamlit.app/` as the fallback base URL for registration invitations whenever `APP_BASE_URL` is not configured.
- Update the admin registration page messaging so it explains that the app is using the fallback cloud URL instead of the old `/` placeholder, while still encouraging a custom/local `APP_BASE_URL` when needed.

45. Invitation placeholder URL hardening:
- Treat legacy/example `APP_BASE_URL` values such as `https://your-app-name.streamlit.app` as invalid placeholders instead of honoring them in generated registration invitations.
- When such a placeholder is present, fall back to the fixed public URL `https://keeping-score-teamweekend-2025-2026.streamlit.app/` so invitation copy never uses the dummy Streamlit example hostname.

46. Winner-reward special expansion:
- Add a new one-time manual match special called `The winner takes it all`, unlocked the first time a participant reaches first place in the ranking.
- When this special is played on a non-draw match, the winning side should receive 5 performance points and the losing side 0, while draws keep the normal scoring.
- Show the new special in the upcoming-match actions, special explanations, admin special overview, and per-person override controls.
- Enforce the existing “at most one copy of the same special at a time” rule more strictly by blocking a second pending activation of the same manual special before the first one resolves.

47. King-of-the-Hill, registration scheduling, navigation cleanup, and registration-score update:
- Add a new transferable `King of the Hill` special that can only be held by the current unique leader, moves to a new leader automatically, and stays with the previous holder only while it is already active in a live match.
- Let admins schedule the registration game with an opening date/time instead of only a simple on/off toggle, and show waiting participants a countdown plus a profile-edit hint while they are blocked.
- Allow registration-gated participants to open their profile page so they can update motto and profile picture before the registration game starts.
- Reduce main-panel quick navigation to icon-only buttons on non-home pages, keep icon+text on the home page, update the navigation/help copy, remove the old participant `Weekend Quick Links`, and refresh the home-page marketing text.
- Change registration-game scoring so each correct answer gives 1.0 point, a correct destination guess instantly awards the remaining unanswered questions at 1.5 points each, and the game intro explains the goal, mechanics, and the “don’t share the location” warning.
- Provide a feasibility assessment for a future admin/participant identity-switch feature without implementing it yet.

48. Compact icon-only top navigation:
- On non-home pages, make the quick-navigation buttons shrink to the icon content width instead of stretching across the full column, while leaving the home-page icon+text buttons unchanged.

49. Inline icon-only top navigation:
- On non-home pages, stop rendering the icon-only quick-navigation buttons inside full-width `st.columns`, and instead place them in a left-aligned horizontal row that wraps naturally, so the icons sit next to each other instead of being spread across the page.

50. Continuous icon-flow navigation:
- On non-home pages, render all icon-only quick-navigation buttons inside one continuous horizontal wrapping container instead of splitting them into several grouped rows, so they break only when the available width runs out.

51. Registration-game content refresh:
- Update registration question 3 to the longer Dutch province riddle while keeping the same correct answer set.
- Replace the answer options for questions 4 and 5 with the newly supplied city/place lists while preserving the intended correct options.
- Update the hint for question 9 to the new phrasing about rhyming with a Groningse heuvel.
